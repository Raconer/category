<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ecommerce.category.mapper.CategoryMapper">
<!-- Create -->
<insert id="insert">
        INSERT INTO category(`name`, `parent`, `order`, `regDate`) 
        VALUES('Hello', 0, 1, '2022-12-26 17:35:04');
</insert>
<!-- Read -->
    <select id="count" resultType="Integer">
        select count(*)
        from category;
    </select>

</mapper>
